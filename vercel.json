{
  "buildCommand": "cd notes && npm install && npm run build",
  "outputDirectory": "notes/dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
